import { createMenuItem } from "./extendedUI";

export const homeItemOne = createMenuItem("HomeTab");
export const homeItemTwo = createMenuItem("");
export const homeItemThree = createMenuItem("");

// need to fix the texts so they look appealing
// need to style the texts too
